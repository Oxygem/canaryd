description "canaryd"
author "Nick @ Oxygem"

respawn

script
    # Load any default envars
    if [ -f /etc/default/canaryd ]; then
        . /etc/default/canaryd
    fi

    # Run canaryd
    exec CANARYD_LOCATION
end script
